# 🚀 プレゼンサイト デプロイ手順

## 方法1: Streamlit Community Cloud（推奨・無料）

### 特徴
- ✅ **無料**で使える
- ✅ **自動スケーリング**（大人数アクセスOK）
- ✅ **HTTPS**対応
- ✅ **3日間以上**の公開可能
- ✅ デプロイが**超簡単**（数クリック）

### 手順

#### 1. GitHubにプッシュ
```bash
cd /home/h-okamoto/デスクトップ/sotuken/flame_analysis

# 変更をコミット
git add .
git commit -m "Add presentation site for deployment"
git push origin main
```

#### 2. Streamlit Community Cloudでデプロイ

1. **https://share.streamlit.io/** にアクセス
2. **GitHubアカウント**でログイン
3. **「New app」**をクリック
4. リポジトリ設定:
   - Repository: `OkamotoHironao/flame_analysis`
   - Branch: `main`
   - Main file path: `presentation_site.py`
5. **「Deploy!」**をクリック

#### 3. 完了！
- 数分でデプロイ完了
- URLが発行される（例: `https://your-app-name.streamlit.app`）
- このURLを発表で共有

### メリット
- **スケーラビリティ**: 100人以上の同時アクセスにも対応
- **安定性**: Streamlit公式の信頼性の高いインフラ
- **メンテナンス不要**: サーバー管理不要
- **無料**: コスト0円

---

## 方法2: ローカルで起動（バックアップ用）

### 発表会場でローカル起動する場合

```bash
# プレゼンサイトを起動
streamlit run presentation_site.py --server.port 8502

# ブラウザで開く
# http://localhost:8502
```

### ネットワーク共有（同じWiFi内の他の人もアクセス可能）

```bash
# IPアドレスを確認
hostname -I

# 起動
streamlit run presentation_site.py --server.address 0.0.0.0 --server.port 8502

# 他の人は以下にアクセス
# http://<あなたのIP>:8502
```

### 注意点
- ⚠️ ローカルは**同時アクセス数に限界**あり（5-10人程度）
- ⚠️ PCがスリープすると停止
- ⚠️ WiFiが必要

---

## 方法3: ngrok（簡易公開）

### 手順

```bash
# ngrokインストール（初回のみ）
# https://ngrok.com/ からダウンロード

# プレゼンサイトを起動（別ターミナル）
streamlit run presentation_site.py --server.port 8502

# ngrokで公開
ngrok http 8502
```

### 発行されるURL
- `https://xxxx-xx-xx-xxx-xxx.ngrok-free.app`
- このURLを共有すれば**世界中からアクセス可能**

### メリット
- ✅ すぐに公開できる
- ✅ HTTPSで安全
- ✅ 中規模（20-30人）なら対応可

### デメリット
- ⚠️ PCを起動し続ける必要がある
- ⚠️ 無料版は接続数に制限
- ⚠️ URLが変わる（再起動すると）

---

## 推奨デプロイ方法

### 🏆 ベストプラクティス

```
1. メイン: Streamlit Community Cloud
   → 発表前日までにデプロイ
   → URLを関係者に共有
   
2. バックアップ: ローカル起動
   → 会場のWiFiが不安定な場合に備える
   → ノートPCで起動しておく
```

### デプロイチェックリスト

- [ ] GitHubにプッシュ完了
- [ ] Streamlit Community Cloudでデプロイ
- [ ] URLにアクセスして動作確認
- [ ] スマホ・タブレットでも表示確認
- [ ] URLを発表資料に記載
- [ ] バックアップ用にローカル起動確認

---

## トラブルシューティング

### Streamlit Cloudでエラーが出る場合

```bash
# requirements_streamlit.txtを確認
cat requirements_streamlit.txt

# 必要に応じて追加
echo "scikit-learn>=1.3.0" >> requirements_streamlit.txt
git add requirements_streamlit.txt
git commit -m "Update requirements"
git push
```

### データファイルが見つからない場合

- `outputs/`, `config/`, `data/processed/`をGitにコミット
- `.gitignore`を確認してデータファイルが除外されていないか確認

```bash
# 大きなファイルを確認
find outputs -type f -size +100M

# Git LFSを使う（100MB以上のファイル）
git lfs install
git lfs track "*.csv"
git add .gitattributes
git commit -m "Add LFS tracking"
```

---

## パフォーマンス最適化

### キャッシュの活用

プレゼンサイトは既に`@st.cache_data`を使用してデータをキャッシュしています。

### 推奨設定

- 画像は最適化済み（PNG, 150dpi）
- JSONファイルは軽量（summary.json < 1MB）
- データフレームは必要最小限

---

## 発表当日の運用

### 1. 事前準備（前日）
```bash
# URLにアクセスして最終確認
curl -I https://your-app-name.streamlit.app

# スクリーンショット撮影（バックアップ）
```

### 2. 発表中
- URLをスライドに表示
- QRコード作成して投影
- スマホでも見やすいことを強調

### 3. 発表後
- アプリは自動的に稼働し続ける
- 3日後に削除したい場合はStreamlit Cloudから手動削除

---

## まとめ

✅ **Streamlit Community Cloud** = 無料・簡単・安定  
✅ **大人数OK**（100人以上対応）  
✅ **3日間以上**公開可能  
✅ **デプロイ時間**: 5分程度  

発表成功を祈ります！ 🎉
